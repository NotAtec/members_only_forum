<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <!-- code for else -->
      <% end %>

      <%= render 'feed' %>
      <%= render 'recommended' %>
    </div>
  </div>
</section>

  
  <div class="column is-half"><%= render "recommended" %></div>
</div>