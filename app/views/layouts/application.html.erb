<!DOCTYPE html>
<html>
  <head>
    <title>Members Only, the best stories!</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if flash[:notice] %>
  		<div class="notification is-primary global-notification">
  			<p class="notice"><%= notice %></p>
  		</div>
  	<% end %>
  	<% if flash[:alert] %>
  		<div class="notification is-danger global-notification">
  			<p class="alert"><%= alert %></p>
  		</div>
  	<% end %>

    <nav class="navbar has-background-primary">
      <div class="navbar-brand">
        <%= link_to root_path, class: "navbar-item" do %>
          <h1 class="title has-text-white">Members Only!</h1>
        <% end %>
        <div class="navbar-burger">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </div>
      <div class="navbar-menu">
        <div class="navbar-start">
          <div class="navbar-item">
            <p class="has-text-white">About</p>
          </div>
        </div>
        <div class="navbar-end">
          <% if user_signed_in? %>
            <div class="navbar-item">
              <p class="control"><%= link_to "New Post", new_post_path, class: "button is-info is-inverted" %></p>
            </div>
            <div class="navbar-item">
              <p class="control"><%= link_to current_user.username, edit_user_registration_path, class: "has-text-white" %></p>
            </div>
            <div class="navbar-item">
              <p><%= link_to "Sign Out", destroy_user_session_path, class: "has-text-white" %></p>
            </div>
          <% else %>
            <div class="navbar-item">
              <p class="control"><%= link_to 'Sign In', new_user_session_path, class: "button is-link" %></p>
            </div>
            <div class="navbar-item">
              <p class="control"><%= link_to 'Sign Up', new_user_registration_path, class: "has-text-white" %></p>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
    <%= yield %>
    <footer>
      <div class="content has-text-centered">
        <p><strong>Members Only!</strong> by <a href="https://github.com/NotAtec">Jeroen Brinkhorst</a></p>
      </div>
    </footer>
  </body>
</html>